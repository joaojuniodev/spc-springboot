cors:
  originPatterns: http://localhost:5500,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:3000,https://spc-azure.vercel.app
spring:
  application:
    name: spc
  jackson:
    time-zone: America/Sao_Paulo
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false